# client-sdk-python

Python SDK for promptLM JSON prompt bundles.

The package exposes a minimal prompt model and loads `prompts/prompt-index.json` plus per-prompt JSON files from either a filesystem bundle or packaged resources.
